<%= form_with model: @room do |form| %>
  <div class="my-3">
    <%= form.label :name %>
    <%= form.text_field :name, required: true, class: "w-full" %>
  </div>
  <div>
    <%= form.submit "Create Room", class: "btn btn-primary w-full" %>
  </div>
<% end %>
