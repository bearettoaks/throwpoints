<div class="flex justify-center items-center w-screen h-screen">
  <header>
    <h1 class="text-2xl font-bold text-center">throwpoints</h1>
    <p class="text-lg text-zinc-700">Create your planning room and invite others.</p>
    <div class="mt-4 text-center">
      <%# <%= button_to "Create a room", rooms_path, class: "btn btn-primary bg-zinc-900 text-zinc-100 px-4 py-2 hover:text-white", data: { turbo: false } %1> %>
      <%= link_to "Create a room", new_room_path, class: "btn btn-primary bg-zinc-900 text-zinc-100 px-4 py-2 hover:text-white" %>
    </div>
  </header>
</div>
